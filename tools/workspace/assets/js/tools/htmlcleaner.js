
import { $, copyText, notify } from '../core/utils.js';
function clean(html=''){ return html.replace(/<script[\s\S]*?<\/script>/gi,'').replace(/<style[\s\S]*?<\/style>/gi,'').replace(/\son[a-z]+=(".*?"|'.*?'|[^\s>]+)/gi,'').replace(/\s(style|class|id|data-[a-z0-9_-]+)=(".*?"|'.*?')/gi,'').replace(/\n{3,}/g,'\n\n').trim(); }
export function render(){ return `<div class="card-grid"><section class="card two-col"><div class="mini-card"><textarea id="hcInput" placeholder="Paste HTML di sini..."></textarea><div class="action-row"><button class="btn primary" id="hcRun">Clean</button><button class="btn ghost" id="hcClear">Clear</button></div></div><div class="mini-card"><textarea id="hcOutput" readonly placeholder="Hasil clean HTML..."></textarea><div class="action-row"><button class="btn primary" id="hcCopy">Copy</button></div></div></section></div>`; }
export function init(){ $('#hcRun').onclick=()=>{ const input=$('#hcInput').value; if(!input.trim()) return notify('Paste HTML dulu.'); $('#hcOutput').value=clean(input); }; $('#hcClear').onclick=()=>{ $('#hcInput').value=''; $('#hcOutput').value=''; }; $('#hcCopy').onclick=()=>copyText($('#hcOutput').value).then(()=>notify('Output dicopy.')); }
